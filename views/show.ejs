<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Suggestion</title>
</head>
<body>
    <h1>Feature Suggestion</h1>
    <div>
        <a href="/feature-requests/<%=company%>">Back</a>
 
    </div>
    <div>
        <h3><%= feature.title %></h3>
        <p><%= feature.description%></p>  
    </div>
    <div>
        <h1>Comments</h1>
        <%for (let i = 0; i < feature.comments.length; i++) { %>
            <div>
                <p> <%= feature.comments[i] %> </p>
            </div>
        <% } %>
        <h1>New Comment</h1>
        <form action="/feature-requests/comments/<%= feature.id %>?_method=PUT" method="POST">
            Comments: <textarea name="comments" id="comments" cols="30" rows="10" required></textarea>
            <input type="submit" name="" value="Submit">
        </form>
    </div>
</body>
</html>